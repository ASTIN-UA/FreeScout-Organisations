# OrgPortal — TODO

---

## Підтримка великих організацій

- [ ] **SLA / пріоритет per org** — поле рівня обслуговування на організації (Premium, Standard тощо); видно агентам для пріоритизації черги
- [ ] **Статистика та звіти** — дашборд менеджера в порталі: відкриті/закриті за період, середній час відповіді, найактивніші автори
- [ ] **Масові дії в порталі** — закрити кілька заявок, перепризначити автора для групи
- [ ] **Bulletin board / оголошення** — адмін публікує повідомлення видиме всім менеджерам організації в порталі (напр. планове обслуговування)
- [ ] **Експорт заявок** — менеджер скачує CSV/Excel заявок організації за вказаний період
- [ ] **Webhooks для організаційних подій** — вихідні webhooks: нова заявка атрибутована до org, member доданий/видалений, org активована/деактивована

---

## Технічний борг (snapshot / tag mode)

- [ ] **Snapshot toggle UX** — зробити "Snapshot Visibility" більш помітним в адмінці коли attribution_source = tag/tag_only
- [ ] **Auto-enable snapshot для tag-режиму** — `snapshotEnabled()` повертає `true` автоматично коли attribution_source = tag/tag_only

---

## Дрібне

- [ ] `notify_on_new_ticket` колонка в `organization_members` — мертва після впровадження
      системи підписок. Прибрати (міграція + fillable/casts).
- [ ] i18n: ключ `notif_scope_no_unit` додати до 16 локалей (є лише в `en` + `uk`):
      `cs, da, de, es, fi, fr, it, ka, nl, no, pl, pt-BR, pt-PT, ro, sk, sv, zh-CN`
- [ ] Рефакторинг `OrgPortalServiceProvider::getLocaleName()` — видалити хардкод список мов.
      `getAvailablePortalLocales()` вже динамічно тягне мови з EUP; `getLocaleName()` мав би
      покладатися тільки на `Helper::getLocaleData()`, а не мати хардкод для синхронізації.

---

## Відкладено (не актуально зараз)

### Опція "Тільки портал організації"

Звичайний член організації при вході в EUP бачить стандартну сторінку "Мої заявки" з усіма заявками без фільтрації по `org_id`. Опція мала б редіректити його одразу на портал організації.

- Чекбокс в адмін System tab: "Тільки портал організації"
- ServiceProvider: хук на EUP tickets route → redirect на `orgportal.portal.company-tickets`
- Розширити Company Tickets: доступ для звичайних учасників (лише свої заявки, без фільтру підрозділу, без зміни автора)

